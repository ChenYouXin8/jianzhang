# 部署说明：让简账在手机上随时可用

本项目是 **PWA（网页应用）**，构建产物是一个纯静态文件夹（`dist/`），
放到任何静态托管上即可，不需要服务器、不需要数据库。

**数据说明**：所有账目只保存在当前设备的浏览器本地（IndexedDB），
默认不上传任何服务器。想多设备互通，可在「设置 → WebDAV 同步」里
配置坚果云（详见 README）；换设备前请先导出备份或完成同步。

---

## 方案 A：Cloudflare Pages（推荐，免费、无需信用卡）

**方式一：连接 GitHub 自动部署（推荐，改代码自动上线）**

1. 把仓库推送到 GitHub，Cloudflare Pages → Create → **Connect to Git** 连接仓库
2. 构建配置：Build command `npm run build`，输出目录 `dist`（仓库内已带 `wrangler.toml`、`.nvmrc`）
3. 之后每次 `git push` 自动构建部署

**方式二：直接上传（Upload assets）**

1. 双击运行 `部署.bat`（会自动构建 + 打开上传页 + 打开 dist 文件夹）
2. 没有账号先免费注册：dash.cloudflare.com（邮箱即可）
3. Workers & Pages → **Create** → **Pages** → **Upload assets**
4. 把 `dist` 文件夹**整个拖进**上传框
5. 十几秒后得到一个网址：`https://xxx.pages.dev` —— 部署完成

以后每次改了代码：重新双击 `部署.bat`，重新拖一次 dist 即可（网址不变）。

## 方案 B：Netlify Drop（备选，同样的拖拽方式）

打开 https://app.netlify.com/drop ，把 `dist` 文件夹拖进去即可。
得到 `https://xxx.netlify.app`。国内访问速度一般，优先用方案 A。

## 手机上安装成「App」

部署后的网址自带 HTTPS，手机浏览器打开即可用，还能添加到主屏幕：

- **iPhone（Safari）**：打开网址 → 底部「分享」按钮 → **添加到主屏幕**
- **Android（Chrome）**：打开网址 → 右上角菜单 ⋮ → **安装应用 / 添加到主屏幕**

之后从主屏幕图标打开，全屏运行、离线可用，和原生 App 一样。

## 常见问题

- **页面更新后手机上还是旧版？** PWA 会自动静默更新，重启应用即可；
  若仍有旧缓存，在手机上删掉浏览器该站点数据后重新打开。
- **电脑端还能用吗？** 能。之前的方式（双击 `start.bat`）不受影响，
  只是电脑和手机默认是两本独立账本。
- **想要手机和电脑数据互通？** 在「设置 → WebDAV 同步」里配置坚果云等
  WebDAV 服务器并开启自动同步，两端即可加密同步同一本账（详见 README）。
